apiVersion: v1
kind: ConfigMap
metadata:
  namespace: metallb-system
  name: config
data:
  config: |
    address-pools:
    - name: cluster-lb-pool
      protocol: layer2
      addresses:
      - {{ lb_pool_start }}-{{ lb_pool_end }}
